//5.4 실행파일
/**
 * (하는일)
 * @param {타입(변수의 타입)} 변수이름(변수의 이름)
 * @returns {타입(반환값)}
 */

//------------------------
/**
 * 초기화 함수
 * @param {object} config 설정 객체
 * @param {boolean} config.debug 설정 변수
 * @param {string} config.url 설정 변수
 * @returns {void}
 */
export function init(conging) {
  return true;
}

/**
 * 프로젝트 종료
 * @param {number} code
 * @returns {number}
 */
export function exit(code) {
  return code + 1;
}
